# Autenticación con PIN — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cambiar la credencial principal de Green Sol de contraseña alfanumérica a un PIN numérico de 6 dígitos (estilo Cashea), dejando la contraseña como factor fuerte opcional para cripto, con bloqueo por intentos y migración suave de usuarios existentes.

**Architecture:** Reusa los campos existentes de `Usuario` (`pinHash` pasa a credencial principal; `hashContrasena` a factor fuerte opcional) y añade campos de bloqueo. El registro se reordena a correo → OTP → PIN → datos → completado; el login pasa a correo → PIN. Los usuarios existentes sin PIN pasan por "Crea tu PIN".

**Tech Stack:** Next.js 16 (Server Actions), Prisma + Postgres, Argon2, Zod, Playwright. Verificar con `timeout 150 npx tsc --noEmit | grep "error TS"` (no `next build` con dev activo; reiniciar dev tras `migrate dev`).

Spec: `docs/superpowers/specs/2026-06-01-auth-pin-design.md`.

---

## Ciclo por tarea
Cada tarea termina con: `tsc --noEmit` limpio → (E2E donde aplique) → commit con versión `0.0.x` + CHANGELOG. Despliegue al VPS (`git pull + up --build` + `docker image prune -f` + `docker builder prune -f`) tras las tareas con cambio visible, según convenga.

---

## Task 1 — Modelo: campos de bloqueo del PIN + migración

**Files:**
- Modify: `prisma/schema.prisma` (model `Usuario`)
- Migration: `prisma/migrations/<ts>_auth_pin/`

- [ ] **Step 1: Añadir campos al modelo `Usuario`**

```prisma
pinIntentos       Int       @default(0)
pinBloqueadoHasta DateTime?
```

- [ ] **Step 2: Migrar + regenerar + reiniciar dev**

Run: `npx prisma migrate dev --name auth_pin`
Luego matar y relanzar `next dev` (cliente Prisma viejo en memoria).

- [ ] **Step 3: tsc + commit**

Run: `timeout 150 npx tsc --noEmit 2>&1 | grep "error TS"` → limpio.
```bash
git add prisma/ && git commit -m "feat(auth): campos de bloqueo del PIN + migración (v0.0.70)"
```

---

## Task 2 — Lógica del PIN (`lib/auth/pin.ts`)

**Files:**
- Create: `lib/auth/pin.ts`
- Test: `e2e/` no aplica (lógica pura; se prueba con un script tsx en Step 3)

- [ ] **Step 1: Implementar `lib/auth/pin.ts`**

```ts
import { hash, verify } from "@node-rs/argon2";
import { prisma } from "@/lib/db";

const MAX_INTENTOS = 5;
const BLOQUEO_MIN = 15;
const PINS_TRIVIALES = new Set(["000000", "111111", "123456", "654321", "121212"]);

/** True si el PIN tiene formato válido (6 dígitos, no trivial). */
export function pinFormatoValido(pin: string): boolean {
  return /^\d{6}$/.test(pin) && !PINS_TRIVIALES.has(pin);
}

export function hashearPin(pin: string): Promise<string> {
  return hash(pin);
}

export type ResultadoPin =
  | { ok: true }
  | { ok: false; error: string; bloqueadoHasta?: Date };

/** Verifica el PIN aplicando bloqueo por intentos. Resetea intentos al acertar. */
export async function verificarPin(usuarioId: string, pin: string): Promise<ResultadoPin> {
  const u = await prisma.usuario.findUnique({ where: { id: usuarioId } });
  if (!u?.pinHash) return { ok: false, error: "Esta cuenta no tiene PIN." };
  if (u.pinBloqueadoHasta && u.pinBloqueadoHasta > new Date()) {
    return { ok: false, error: "Demasiados intentos. Espera unos minutos.", bloqueadoHasta: u.pinBloqueadoHasta };
  }
  const ok = await verify(u.pinHash, pin);
  if (ok) {
    if (u.pinIntentos !== 0 || u.pinBloqueadoHasta) {
      await prisma.usuario.update({ where: { id: usuarioId }, data: { pinIntentos: 0, pinBloqueadoHasta: null } });
    }
    return { ok: true };
  }
  const intentos = u.pinIntentos + 1;
  const bloquea = intentos >= MAX_INTENTOS;
  const bloqueadoHasta = bloquea ? new Date(Date.now() + BLOQUEO_MIN * 60_000) : null;
  await prisma.usuario.update({
    where: { id: usuarioId },
    data: { pinIntentos: bloquea ? 0 : intentos, pinBloqueadoHasta: bloqueadoHasta },
  });
  return bloquea
    ? { ok: false, error: "Demasiados intentos. Espera 15 minutos.", bloqueadoHasta: bloqueadoHasta! }
    : { ok: false, error: `PIN incorrecto. Te quedan ${MAX_INTENTOS - intentos} intentos.` };
}
```

- [ ] **Step 2: Verificar formato con un check rápido**

Run: `npx tsx -e "import {pinFormatoValido} from './lib/auth/pin'; console.log(pinFormatoValido('123456'), pinFormatoValido('482913'), pinFormatoValido('12a456'))"`
Expected: `false true false`

- [ ] **Step 3: tsc + commit**

```bash
git add lib/auth/pin.ts && git commit -m "feat(auth): lógica de PIN con bloqueo por intentos (v0.0.71)"
```

---

## Task 3 — Schemas Zod del PIN (`lib/validations/auth.ts`)

**Files:**
- Modify: `lib/validations/auth.ts`

- [ ] **Step 1: Añadir schema del PIN y del registro con PIN**

```ts
export const pinSchema = z
  .object({ pin: z.string().regex(/^\d{6}$/, "El PIN es de 6 dígitos"), confirmar: z.string() })
  .refine((d) => d.pin === d.confirmar, { message: "Los PIN no coinciden", path: ["confirmar"] });

// Datos del registro (sin credencial; el PIN se define en su propio paso)
export const registroDatosSchema = z.object({
  nombre: z.string().min(1, "Indica tu nombre"),
  apellido: z.string().min(1, "Indica tu apellido"),
  nombreUsuario: z.string().min(3, "Mínimo 3 caracteres").max(15, "Máximo 15 caracteres").regex(/^[a-zA-Z0-9_]+$/, "Solo letras, números y guion bajo"),
  pais: z.string().min(2, "Elige tu país"),
});

// Login: correo/usuario + PIN
export const loginPinSchema = z.object({
  identificador: z.string().trim().min(1, "Correo o nombre de usuario"),
  pin: z.string().regex(/^\d{6}$/, "PIN de 6 dígitos"),
});
```

(Mantener `contrasenaSchema`/`loginSchema` para la migración y el factor fuerte.)

- [ ] **Step 2: tsc + commit**

```bash
git add lib/validations/auth.ts && git commit -m "feat(auth): schemas de PIN, registro y login con PIN (v0.0.72)"
```

---

## Task 4 — Componente `CampoPin` (input de 6 dígitos)

**Files:**
- Create: `components/campo-pin.tsx`

- [ ] **Step 1: Implementar `CampoPin`** (client). 6 casillas numéricas, `inputMode="numeric"`, avanza al siguiente dígito automáticamente, expone el valor por `onChange(pin: string)`. Prop `onCompleto?: () => void` (para auto-validar al 6º dígito). Reutilizable en login, registro y migración. Usa el estilo de marca (casillas redondeadas, `border-brand` al activo). `testId` para QA.

- [ ] **Step 2: tsc + commit**

```bash
git add components/campo-pin.tsx && git commit -m "feat(auth): componente CampoPin de 6 dígitos (v0.0.73)"
```

---

## Task 5 — Login con PIN

**Files:**
- Modify: `app/(auth)/actions.ts` (`iniciarSesion`)
- Modify: `app/(auth)/login/page.tsx` (dos pasos: correo → PIN)

- [ ] **Step 1: Reescribir `iniciarSesion`** para validar con `loginPinSchema`, buscar usuario por correo (lowercase) o `nombreUsuario` (insensitive), y verificar con `verificarPin`. Si el usuario **no tiene `pinHash`** (migración pendiente) → devolver una señal para redirigir a "Crea tu PIN" (Task 7). Mantener el incremento de `ingresos` y el redirect a onboarding/dashboard.

```ts
const usuario = await prisma.usuario.findFirst({
  where: { OR: [{ correo: identificador.toLowerCase() }, { nombreUsuario: { equals: identificador, mode: "insensitive" } }] },
});
if (!usuario) return { error: "Correo/usuario o PIN incorrectos." };
if (!usuario.hashContrasena && !usuario.pinHash) return { error: "Cuenta inválida." };
if (!usuario.pinHash) return { error: "MIGRAR_PIN" }; // login/page redirige a crear PIN
const r = await verificarPin(usuario.id, pin);
if (!r.ok) return { error: r.error };
if (!usuario.correoVerificado) { /* enviar OTP y redirect /verificar, como hoy */ }
// incrementar ingresos + crearSesion + redirect (igual que hoy)
```

- [ ] **Step 2: Reescribir `login/page.tsx`** a dos pasos: paso 1 correo (input, "Siguiente"), paso 2 `CampoPin` + "Entrar". Conservar el correo al fallar (ya se hace). Mostrar "¿Olvidaste tu PIN?" como "Pronto" (recuperación fuera de alcance). Si `estado.error === "MIGRAR_PIN"`, redirigir a `/migrar-pin?u=<identificador>`.

- [ ] **Step 3: E2E** — actualizar `e2e/publico.spec.ts` y crear `e2e/auth-pin.spec.ts`: registro con PIN (Task 6) + login con PIN. Por ahora un smoke: el endpoint `/api/test/sesion` sigue creando sesión directa para los demás E2E (no usa PIN), así que esos no se rompen.

- [ ] **Step 4: tsc + commit**

```bash
git add app/\(auth\)/actions.ts app/\(auth\)/login/page.tsx e2e/ && git commit -m "feat(auth): login con PIN + bloqueo por intentos (v0.0.74)"
```

---

## Task 6 — Registro reordenado (correo → OTP → PIN → datos → completado)

**Files:**
- Modify: `app/(auth)/actions.ts` (`solicitarRegistro`, `definirPinRegistro`, `completarRegistro`)
- Modify: `app/(auth)/registro/page.tsx` (pasos)
- Modify: `app/(auth)/verificar/page.tsx` (tras OTP → ir al PIN, no al dashboard)
- Create: `app/(auth)/registro/completado/page.tsx` (pantalla de éxito + pop-up biometría)

- [ ] **Step 1: `solicitarRegistro(correo)`** — valida correo, comprueba que no exista verificado, crea/actualiza un usuario pendiente (solo correo), envía OTP (`crearYEnviarOtp`), guarda cookie `PENDIENTE`, redirige a `/verificar`.
- [ ] **Step 2: `verificarCodigo`** (existente) — al validar el OTP, marca `correoVerificado = true` y redirige a `/registro` paso PIN (en vez de al dashboard). El `registro/page` detecta correo verificado + sin PIN → muestra el paso PIN.
- [ ] **Step 3: `definirPinRegistro(pin, confirmar)`** — valida con `pinSchema` + `pinFormatoValido`, guarda `pinHash` en el usuario pendiente (de la cookie), pasa al paso de datos.
- [ ] **Step 4: `completarRegistro(datos)`** — valida `registroDatosSchema` + restricciones + unicidad de usuario (insensitive), guarda nombre/apellido/usuario/país, crea la notificación de "completa tu verificación", limpia la cookie, redirige a `/registro/completado`.
- [ ] **Step 5: `registro/page.tsx`** — wizard de pasos: correo → (OTP en `/verificar`) → PIN (`CampoPin` + confirmar) → datos (nombre, apellido, usuario con validación en vivo, país). Reusa el patrón de pasos existente.
- [ ] **Step 6: `registro/completado/page.tsx`** — "¡Tu cuenta está lista! 🎉" + botón "Iniciar sesión" (→ `/login`) + pop-up visual "Usaremos tu biometría para mayor seguridad" con "Entendido" (solo informativo).
- [ ] **Step 7: E2E** `e2e/auth-pin.spec.ts` — registro completo: correo → (OTP: leer el código del log/endpoint de test) → PIN → datos → completado → login con PIN → dashboard.
- [ ] **Step 8: tsc + suite E2E + commit**

```bash
git add app/\(auth\)/ e2e/ && git commit -m "feat(auth): registro con OTP→PIN→datos + pantalla completado (v0.0.75)"
```

---

## Task 7 — Migración: "Crea tu PIN" para usuarios existentes

**Files:**
- Create: `app/(auth)/migrar-pin/page.tsx`
- Modify: `app/(auth)/actions.ts` (`crearPinMigracion`)

- [ ] **Step 1: `crearPinMigracion(identificador, contrasena, pin, confirmar)`** — busca el usuario, verifica la **contraseña actual** con `verificarContrasena`, valida el PIN (`pinSchema` + `pinFormatoValido`), guarda `pinHash`. La `hashContrasena` se conserva (factor fuerte). Devuelve ok → login.
- [ ] **Step 2: `migrar-pin/page.tsx`** — "Crea tu PIN": input de contraseña actual + `CampoPin` (pin + confirmar). Al éxito, redirige a `/login` (o crea sesión directa). Llega aquí desde el login cuando `error === "MIGRAR_PIN"`.
- [ ] **Step 3: E2E** — en `auth-pin.spec.ts`, caso: usuario con contraseña sin PIN → al loguear, va a crear PIN → define PIN → entra.
- [ ] **Step 4: tsc + E2E + commit**

```bash
git add app/\(auth\)/ e2e/ && git commit -m "feat(auth): migración 'crea tu PIN' para usuarios existentes (v0.0.76)"
```

---

## Task 8 — Limpieza: el PIN ya no es "2FA opcional"

**Files:**
- Modify: `components/form-seguridad.tsx` (la tarjeta de PIN ya no es 2FA; el PIN es la credencial)
- Modify: `components/seccion-verificacion.tsx` + dashboard/perfil (el paso "2FA" se replantea)
- Modify: `lib/seguridad.ts` (`verificarFactores` queda solo para el futuro modal de acciones sensibles)

- [ ] **Step 1:** En **Seguridad**, el PIN deja de presentarse como "segundo factor" (ya es el login). La sección pasa a ofrecer: **cambiar PIN**, y **configurar contraseña/biometría** como factor fuerte para cripto (la contraseña con su política actual). El "código por correo (OTP)" se mantiene como verificación de acciones, no como login.
- [ ] **Step 2:** Ajustar `seccion-verificacion.tsx` y el banner: el contador `/3` pasa a (correo verificado · PIN creado · identidad KYC) — el PIN ya lo tienen todos, así que el paso 2 se da por hecho tras el registro. Revisar `tiene2FA` en dashboard/perfil para que refleje esto sin romper el banner del KYC (Task previa ya hecha).
- [ ] **Step 3:** `cambiarContrasena` (en configuracion/actions) se mantiene para el factor fuerte; ya no exige 2FA para cambiarla (esa regla era del modelo viejo). Revisar y simplificar.
- [ ] **Step 4: tsc + suite E2E + commit + docs**

Actualizar `PRD.md`/`PRD.html`/`ARQUITECTURA` (auth con PIN) y CHANGELOG.
```bash
git add -A && git commit -m "refactor(auth): PIN es la credencial (no 2FA); contraseña = factor fuerte; docs (v0.0.77)"
```

---

## Self-review (cobertura de la spec)

- PIN credencial principal (registro + login) → Tasks 5, 6 ✓
- Contraseña = factor fuerte opcional (cripto, futuro) → modelo (Task 1) + Task 8 ✓
- Bloqueo por intentos (5 → 15 min) → Tasks 1, 2 ✓
- Validación PIN (6 dígitos, no trivial, Argon2) → Task 2 ✓
- Registro correo → OTP → PIN → datos → completado + pop-up biometría → Task 6 ✓
- Login correo → PIN → Task 5 ✓
- Migración "Crea tu PIN" → Task 7 ✓
- Componente reutilizable de PIN → Task 4 ✓
- Fuera de alcance (recuperación PIN, biometría real, teléfono, modal cripto) → no se implementan (documentado) ✓
